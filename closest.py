def populate(n,i,j):
    """
    This will just populate the list L with random numbers between i and j. It
    will populate n numbers
    """
    L=[]
    for i in range(n):
        L.append(random.randint(i,j))
    return L

def new_func

